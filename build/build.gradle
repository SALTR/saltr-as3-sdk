buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath group: 'org.gradlefx', name: 'gradlefx', version: '1.0'
    }
}

apply plugin: 'gradlefx'

srcDirs = ['../src']


//flexHome='/Users/daal/Projects/dev/as/flash-sdk/mac/flex_sdk_4.6'

output='../../bin/saltr-sdk.v.1.1.2'

additionalCompilerOptions = [
    "-load-config=air-config.xml"
]

type = 'swc'